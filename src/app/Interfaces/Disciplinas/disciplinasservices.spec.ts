import { Disciplinasservices } from './disciplinasservices';

describe('Disciplinasservices', () => {
  it('should create an instance', () => {
    expect(new Disciplinasservices()).toBeTruthy();
  });
});
